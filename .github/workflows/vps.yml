name: GitHub-VPS
on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: VPS Qurulur
        run: |
          Invoke-WebRequest https://bin.equinox.io/c/4VmDzA7iaHb/ngrok-stable-windows-amd64.zip -OutFile ngrok.zip
          Expand-Archive ngrok.zip
          net user runneradmin "Vps12345!" /add
          net localgroup administrators runneradmin /add
          ./ngrok/ngrok.exe authtoken "3BcGhxJdpZFxKdIeYYhLTqoIYmJ_4kWcZoJk4xPmmVpVn8pNW"
          
          # Ngrok-u birbaşa terminalda başladırıq (Log gözləmədən)
          Write-Host "🚀 VPS Başladılır... 10 saniyə gözləyin."
          Start-Process ./ngrok/ngrok.exe -ArgumentList "tcp 3389"
          Start-Sleep -Seconds 15
          
      - name: Linki Çıxar
        run: |
          # Ngrok-un rəsmi API-sindən linki çəkmək üçün 3 cəhd edirik
          for ($i=1; $i -le 5; $i++) {
              try {
                  $content = Invoke-WebRequest -Uri http://localhost:4040/api/tunnels -UseBasicParsing
                  $url = ($content.Content | ConvertFrom-Json).tunnels[0].public_url
                  if ($url) { 
                      Write-Host "✅ TAPILDI: $url"
                      break 
                  }
              } catch { 
                  Write-Host "⏳ Cəhd $i: Gözlənilir..."
                  Start-Sleep -Seconds 5 
              }
          }
          Write-Host "=========================================="
          Write-Host "🔗 ÜNVAN: $url"
          Write-Host "👤 AD: runneradmin | 🔑 ŞİFRƏ: Vps12345!"
          Write-Host "=========================================="
          while($true) { Start-Sleep -Seconds 3600 }
